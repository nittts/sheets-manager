export const enum Permissions {
  // Read self sheets
  READ_SHEETS = 10,

  // Read other users sheets
  READ_PLAYER_SHEET = 11,

  // Create self sheet
  CREATE_SHEET = 20,

  // Create a new game session with sheets
  CREATE_SESSION = 30,
}
